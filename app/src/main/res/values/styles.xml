<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">@color/colorText</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/myCheckBoxColor</item>
    </style>


    <style name="NavyText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#071f5d</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">@color/colorText</item>


    </style>

    <style name="MyCustomDialogStyle" parent="@android:style/Theme.DeviceDefault.Dialog.NoActionBar.MinWidth">

        <!-- Used for the background-->
        <item name="android:background">#071f5d</item>

        <!-- Used for the title and text-->
        <item name="android:textColorPrimary">#ffffff</item>

        <!-- title color + button color? -->
        <item name="android:textColor">#ffffff</item>

    </style>


</resources>
